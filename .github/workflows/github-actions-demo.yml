name: CI
on:
  push:
    branches: [master]
jobs:
  codecommit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello-world
        shell: bash
        run: echo hello
